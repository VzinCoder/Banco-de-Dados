Create database projeto_inicial;

use projeto_inicial;

create table cliente (
Nome varchar(30) primary key,
Email varchar(30),
Telefone varchar(30),
Sexo char(1),
Endereco varchar(100),
cpf Int(11)
);

